# Implementation state — pre-Python-migration fixes

**Campaign:** `prompts/pre-python-migration-fixes/`
**Last updated:** 2026-09-05 (prompt 07 **Complete**: campaign closed. `baseline/post-fix` archived and tagged; the full comparison matrix run; `SUMMARY.md` and `validation/ORACLE.md` written; issue register reconciled; I-11 closed)
**Updated by:** implementing agent

> Every prompt must edit this file twice: once on entry (status → *in flight*) and once on exit
> (status → *complete*, with the commit SHA). Keep the tables and the issue register current; this
> is the only place that reports campaign-wide state.

---

## 1. Prompt status

| # | Prompt | Status | Branch | Commit | Log | Notes |
|---|---|---|---|---|---|---|
| 01 | Remove Boost; C++17 build | **Complete** | `pre-python-migration-fixes/01-deboost-and-build` | `2e75f7f2` | `logs/01-deboost-and-build.md` | Verified: 101 kernels, 135 `compute_*` names (identical to 2018 reference), 22 `autogenerated/` files, exactly 3 expected warnings, ~44-47s wall clock |
| 02 | Oracle harness + pre-fix baseline | **Complete** | `pre-python-migration-fixes/02-oracle-harness-and-baseline` | `0a0b1e0b` | `logs/02-oracle-harness-and-baseline.md` | Branched off `d8d4532`. **The 2026 build reproduces the 2018 published output as functions: 101 vs 101 kernels, 101/101 signature vectors matched on the exact key, `compute_*` sets identical at 135 names.** Harness under `validation/`; baseline archived and tagged `baseline/pre-fix`; dedup confirmed 101 → 95 with 6 pairs; `--auto-symmetrize` measured at 23× for identical output |
| 03 | Determinism | **Complete** | `pre-python-migration-fixes/03-determinism` | `cdde7158` | `logs/03-determinism.md` | Branched off `6b9a0a1`. **Full byte-reproducibility achieved**: five independent runs now give a byte-identical `autogenerated/` tree (21/21 non-timestamp files), excluding only the generation timestamp (by design). Functionally verified unchanged against `baseline/pre-fix` and the 2018 reference tree. Counterterm map content stable via canonicalised comparison (raw text still process-varying, as foreseen). New issue I-10 opened (the `--counterterms` toggle still perturbs GiNaC's internal print order, not content; out of scope here) |
| 04 | Wick Tier-1 fixes | **Complete** | `pre-python-migration-fixes/04-wick-tier1-fixes` | `b065fdd` | `logs/04-wick-tier1-fixes.md` | Branched off `f4d100f`. **All three fixes landed in one commit; result is stronger than regression-free — the emitted `autogenerated/` tree is byte-identical (not just functionally identical) before and after the fix.** Branch counts matched the study exactly: `t1_simple` 0→678, `fallthrough` 776→98, 390 groups/776 contractions throughout. Verified against `baseline/pre-fix` (101/101 signatures, 135 `compute_*` names, warning set, counterterm map) and the 2018 reference tree (101/101) |
| 05 | Numeric/shape hygiene | **Complete** | `pre-python-migration-fixes/05-numeric-and-shape-hygiene` | `2e95be9` | `logs/05-numeric-and-shape-hygiene.md` | Branched off `8d72e7e`. **All three fixes verified regression-free: emitted `autogenerated/` tree byte-identical (not just functionally identical) before/after.** `A(r)` replaced with exact closed form `C(2r,r)/2^r` (regression test: 24 checks, `A(11)=88179/256` exact); `double_factorial(num-1)` guarded against `num==0` with new `ERROR_ZERO_FIELD_CONTRACTIONS`; three Legendre-shape destructurings unified into one shared `extract_Legendre_factors` helper, raising on unhandled shapes. Instrumented run confirms all three previously-dead branches remain untaken on the current operator basis (101/101 signatures, 135 `compute_*` names, warning set, counterterm map, dedup 101→95/6 pairs, all identical to `baseline/pre-fix` and the 2018 reference tree) |
| 06 | Rayleigh-zero diagnosis | **Complete** | `pre-python-migration-fixes/06-rayleigh-zero-diagnosis` | `c88776f` (diagnosis, aliasing fix); `5e2b208` (verdict reversed, findings §8); `69cae4a` (assertion) | `logs/06-rayleigh-zero-diagnosis.md` | Branched off `cc982d5`. **Diagnosis complete; the study's proposed invariant does not hold.** Confirmed 128 zero-valued Rayleigh rules per run (matches study exactly), all created inside `Pk_one_loop::cross_product` (never during kernel construction), from `alpha` (40) and `InverseLaplacian` (88) — `beta` contributes none. Mechanism: a Wick self-contraction pins two of a kernel's own linear legs to equal-and-opposite loop momentum. Tested "label occurs in K" (prune_Rayleigh_list's own test) at the one meaningful pipeline stage, refined with a denominator-specific check: **fails in 92 of 128 cases (71.9%)**, all confirmed genuine denominator occurrences. Per the prompt's own instruction for this outcome, no assertion or eager pruning was implemented; only the aliasing fix (`merge_Rayleigh_rules`'s `-value` search guarded against `value == 0`) was made, verified byte-identical output before/after. See `validation/RAYLEIGH-ZERO-FINDINGS.md` for the full write-up and the open physics question (§3.4) this hands to a future prompt/the port **Resolved 2026-09-05 (interactive session, not a prompt):** three further probes showed the 92 surviving labels multiply identically-zero terms whose cancellation needs `(L0.i)^2 (L0.j)^(-2) = 1`; GiNaC's indexed algebra does not perform it, `dot_products_to_cos(K).expand()` at the top of `one_loop_reduced_integral` does (92 → 70 after `Pk_db::emplace` merging → 0 after cosine conversion). The invariant therefore **holds** for all 128; the 2026-09-04 test was applied to an indexed representation in which the vanishing is not manifest. Findings document revised in place with a new §8; log carries an addendum. Status left as *Blocked* only in the sense that the prompt's own step-3 items were never executed; there is nothing left to diagnose **Step 3 closed 2026-09-05:** item 1 (assert the invariant) implemented at the only place it holds — the `one_loop_reduced_integral` constructor immediately after `dot_products_to_cos(K).expand()` — throwing the new `ERROR_RAYLEIGH_ZERO_LABEL_SURVIVES`; verified byte-identical emitted tree against the `5e2b208` tree, 101/101 signatures vs `baseline/pre-fix`, dedup 101→95/6, counterterm map and warning set identical, and demonstrated to fire (run aborts) when the same check is placed before the cosine conversion. Item 2 (eager pruning in `remove_Rayleigh_trivial`) deliberately **not** done: that function prunes by substitution, and substituting `label -> 0` into `pow(indexed(label,i), -2)` makes GiNaC throw `power::eval(): division by zero`; zero-valued rules are harmless in the list and are never selected. Item 3 (aliasing) was done by the prompt |
| 07 | Close-out and handover | **Complete** | `pre-python-migration-fixes/07-closeout-and-handover` | `7b75eb45` (substantive; see `logs/07-closeout-and-handover.md` §3.1 for why the smoke-test result and tag placement needed two further small commits) | `logs/07-closeout-and-handover.md` | Branched off `74bc284`. **Campaign closed.** `baseline/post-fix` archived (101/101 signatures vs `baseline/pre-fix` and the 2018 reference tree; 21/21 non-timestamp files byte-identical across 5 runs; textually differs from `baseline/pre-fix` in kernel numbering and exactly 47 of 101 `value_` expressions (I-11), sorted `Wick_` set unchanged; byte-identical to the `5e2b208` tree). `SUMMARY.md` and `validation/ORACLE.md` written. Clean-checkout smoke test at tag `baseline/post-fix`: see `logs/07-closeout-and-handover.md` |

Statuses: **Not started** / **In flight** / **Complete** / **Blocked** / **Abandoned**.

---

## 2. Baselines

| Tag | Code state | Location | Contents | Captured |
|---|---|---|---|---|
| `baseline/pre-fix` | after prompt 01 (binary built from `d8d4532`) | `validation/baselines/pre-fix/` | `autogenerated/` tree (22 files, 554,769 B), `run.log`, `manifest.json`, `signatures.json` (101 signatures, seed 20260904), `warnings.txt` (3), `counterterm-map.canonical.txt` (print-order canonicalised — one md5 across six processes), `counterterm-map.sorted-lines.txt` (raw, not comparable) | **2026-09-04**, prompt 02 |
| `baseline/post-fix` | after prompt 06 **and** the 2026-09-05 follow-ups (`5e2b208` FabJ closed form; the assertion commit `69cae4a`); binary built from `74bc284` | `validation/baselines/post-fix/` | `autogenerated/` tree (22 files, 549,382 B), `run.log`, `manifest.json`, `signatures.json` (101 signatures, seed 20260904), `warnings.txt` (3, identical text to `baseline/pre-fix`), `counterterm-map.canonical.txt` (md5 `1c177140d3688e558b445609f8c585a4` — identical to `baseline/pre-fix`), `counterterm-map.sorted-lines.txt` (raw, not comparable) | **2026-09-05**, prompt 07 |

External reference, not produced by this campaign and not to be modified:
`~/Documents/Code/LSSEFT/autogenerated/` — the 2018 published output (101 kernels,
135 `compute_*` functions).

**Reproduction of the 2018 reference is established.** Prompt 02 compared a fresh de-Boosted 2026
run against it with `validation/compare_autogenerated.py`: 101 kernels on each side, **101 of 101
signature vectors matched** on the exact key (no tolerance fallback), `compute_*` name sets
identical at 135 names, seed `20260904`, 25 evaluation points. The two trees are textually
different (a 2018 GiNaC printed the same expressions with different operand order) and functionally
identical. See `logs/02-oracle-harness-and-baseline.md` §4.1.

The tooling that produces these numbers lives in `validation/`; `validation/README.md` explains how
to build, run, compare and re-baseline.

**The post-fix comparison matrix (prompt 07), in full:**

| Comparison | Result |
|---|---|
| post-fix vs `baseline/pre-fix`, functional | MATCH — 101 of 101 signatures, `compute_*` identical (135 names), warning set identical, counterterm map (canonicalised) identical md5 |
| post-fix vs 2018 published, functional | MATCH — 101 of 101 signatures, `compute_*` identical (135 names) |
| post-fix vs post-fix, five runs, textual | IDENTICAL excluding the timestamp — 21 of 21 non-timestamp files (`validation/check_determinism.sh`: PASS) |
| post-fix vs `baseline/pre-fix`, textual | Differs in kernel numbering **and** in exactly 47 of 101 `value_` expressions (I-11, the FabJ closed-form change); sorted `Wick_` set byte-identical |
| post-fix vs the tree emitted at `5e2b208`, textual | IDENTICAL excluding the timestamp — 21 of 21 non-timestamp files (confirms `69cae4a` changed no artefact) |

Full detail, exact commands, and the clean-checkout smoke test: `logs/07-closeout-and-handover.md`.

---

## 3. Issue register

Open items, whether raised by the study, by reconciliation, or by an implementing agent. Add rows;
do not delete them. Close by moving the row to §4.

| ID | Raised by | Severity | Description | Owner prompt |
|---|---|---|---|---|
| I-01 | reconciliation §3 | **High** | GiNaC 1.8.10 randomises expression hashing per process, so ordering inside default-printed expressions varies run to run. Full byte-reproducibility of the counterterm map may be unattainable without a canonicalising printer. **Evidence recorded by prompt 02 in `validation/NONDETERMINISM.md`** (five runs): 10 of 22 files vary for ordering reasons, `kernel_integrands.cpp` gave 5 distinct md5s in 5 runs, the sorted counterterm map 4. Stable: the sorted `value_`/`Wick_` sets, the `compute_*` set, the warning set, the functional signature multiset, and the print-order-canonicalised counterterm map. **New findings for prompt 03:** (a) the counterterm map's variability is *intra-line* (GiNaC's own default-print operand order), so sorting lines is **not** a canonicalisation and that residue is not reachable by sorting our own containers; (b) the whole of it is nonetheless *commutative reordering* — a purely textual print-order canonicalisation (`validation/canonical_form.py --counterterm-map`) collapses all five runs **and** the `--auto-symmetrize` run to one md5, so the map's content is verifiably stable and a regression test on it exists today; (c) every file that varies references `kerNNN` (101–1014 times), every stable file references none, so the instability is entirely kernel naming and ordering. | 03 |
| I-02 | reconciliation §4 | Low | Every run emits `warning: Kernel does not depend on available momentum vector q0` (`lib/fourier_kernel.cpp:657`). Never explained. **Captured by prompt 02** in `validation/baselines/pre-fix/warnings.txt` and `validation/baselines/post-fix/warnings.txt`; present once in each run, alongside the two expected `bG3` messages, and the warning set is identical in both baselines. A change is now detectable. Investigate only if it changes. | **Handed to the port**, written up in `validation/ORACLE.md` §5 |
| I-05 | study §3.4 | Low | 6 of 101 emitted integrands are exact algebraic duplicates that the string-hash dedup misses. **Numbers confirmed by prompt 02** with `validation/canonical_form.py --dedup`: 101 emitted → 95 distinct, 6 duplicate pairs, reproduced on two independent fresh 2026 trees, on `baseline/post-fix`, *and* on the 2018 reference tree. The `kerNNN` labels differ between trees (numbering non-determinism, I-01); the pair structure does not. **Out of scope** for this campaign — belongs to the port. Not acted on. | **Handed to the port**, written up in `validation/ORACLE.md` §5 |
| I-06 | study §6 | Medium | `is_IR_safe()` (`backends/LSSEFT.cpp:435-461`) tests the wrong limits and has never fired. **Out of scope** — the study specifies the fix for the port, not the oracle. | **Handed to the port**, written up in `validation/ORACLE.md` §5 |
| I-09 | study §5.2 | Low | The `8π²` normalisation applied at `backends/LSSEFT.cpp:793` is invisible in the emitted code. Documented in the handover; silently dropping it would rescale every downstream result. | **Handed to the port**, written up in `validation/ORACLE.md` §5 |
| I-10 | prompt 03 measurement | Low | Toggling `--counterterms` still perturbs code generation, confirming the study's original flag: `kernel_integrands.cpp` from a `--counterterms` run differs (in GiNaC's own intra-expression operand order only — not content) from a `--no-counterterms` run of the same binary, even though same-flags runs are now byte-identical (I-01 is closed for that case). Functionally identical (101/101 matched, `compute_*` identical) via `validation/compare_autogenerated.py`. Likely mechanism: `--counterterms` prints the counterterm map (touching/caching GiNaC hash values via `get_UV_limit`/`.expand()`/`collect_common_factors`) *before* the `LSSEFT` backend is built, so GiNaC's per-object hash cache is populated in a different order than on a `--no-counterterms` run — execution-path-dependent, not literally per-process-random. See `validation/NONDETERMINISM.md` §0.3 and `logs/03-determinism.md`. Out of scope for prompt 03 (which only guarantees determinism at fixed flags, the configuration every other tool in this campaign actually uses); not acted on. | **Handed to the port**, written up in `validation/ORACLE.md` §5 |

---

## 4. Closed issues

| ID | Closed by | Resolution |
|---|---|---|
| I-01 | prompt 03 | **Closed for kernel naming/ordering; narrowed and re-opened as I-10 for the `--counterterms` toggle.** `validation/check_determinism.sh`: five independent, identically-configured runs now produce a byte-identical `autogenerated/` tree in full — 21 of 21 non-timestamp files match (up from 11 of 21 stable / 10 of 21 unstable at prompt 02), excluding only `pipeline_id.cpp` and the `// Generated at` header line, both of which are timestamps by design and were never supposed to be reproducible. The counterterm map's raw (default-printed) text remains not byte-reproducible — this was foreseen and is not a regression, since GiNaC's own intra-expression operand order is not reachable by sorting our own containers; its print-order-canonicalised form (`validation/canonical_form.py --counterterm-map`) remains stable at one md5, exactly as at prompt 02. Verified functionally unchanged against `baseline/pre-fix` (101/101 signatures matched, 135 `compute_*` names identical, warning set identical) and against the 2018 reference tree (101/101). See `logs/03-determinism.md` for full detail, including the `canonicalize_Rayleigh_labels()` and counterterm-map implementation choices. |
| I-04 | prompt 04 | Demonstrated, not merely claimed: with fixes (i)+(ii)+(iii) applied together, `t1_simple` measures exactly 678 (the study's number, reproduced exactly) and `fallthrough` exactly 98, out of 776 contractions across 390 groups — both unchanged from before the fix. Functional equality confirmed against `baseline/pre-fix` (101/101 signatures, 135 `compute_*` names, warning set, counterterm map) and the 2018 reference tree (101/101). Beyond the demonstration asked for: the emitted `autogenerated/` tree is byte-identical (not just functionally identical) before and after the fix, because `loop_integral::match_Wick_to_Rayleigh()` re-canonicalises the `Pk` argument downstream of the choice this fix changes. See `logs/04-wick-tier1-fixes.md` §4-§5. |
| I-07 | prompt 04 | All six `_sym`→`_mom` sites were changed in this repository — not only the four the `tree-bispectrum` branch fixes. Confirmed individually (`logs/04-wick-tier1-fixes.md` §2.1) and evidenced by the `[INSTR-KEY]` diagnostic (`mma_map[left_clust].find(left_sym)` fails, `find(left_mom)` succeeds) and by `t1_simple` becoming live at exactly the study's predicted count. `t2_simple` remaining at 0 is expected for a two-point function, not evidence that its site (`:539`) is unfixed — both `:531` and `:539` were changed identically. |
| I-08 | prompt 05 | `A(r)`'s unsigned-int numerator overflows silently from r = 11 (`A(11)` computed as `21.655...` against the correct `344.44921875`) and the denominator from r = 13. Replaced with the exact closed form `A(r) = C(2r,r)/2^r` using `GiNaC::binomial`/`GiNaC::pow`'s exact-rational `numeric` overloads, correct at every r including r = 0 with no loop. Verified against 21 hand-derived exact rationals (`validation/test_neumann_adams.cpp`, 24 checks, all passed) and confirmed regression-free: the emitted `autogenerated/` tree is byte-identical before/after (instrumented run reaches only r ≤ 4 today, per the study). See `logs/05-numeric-and-shape-hygiene.md` §4, §5. |
| I-03 | interactive session 2026-09-05 (after prompt 06) | **Closed: the invariant holds.** The 92 'violations' recorded by prompt 06 are terms whose coefficient is identically zero but whose vanishing requires `(L0.i)^2 * (L0.j)^(-2) = 1` (same loop momentum squared under two dummy indices, from adjacent `Laplacian`/`InverseLaplacian` factors). Neither `simplify_indexed` nor `.normal()` cancels this, which is why the `cross_product`-stage tests saw the label; `dot_products_to_cos(K).expand()` at the top of the `one_loop_reduced_integral` constructor rewrites both as scalar `L0^2` and the terms cancel before `reduce()` classifies anything (checkpoints: 92 → 70 → 0). The pipeline is exactly safe, by design: labels only in denominators, numerators in the underlying momenta, scalar conversion before Rayleigh selection. An assertion is safe only after the cosine conversion; pruning by substitution (`label -> 0`) would make GiNaC throw `power::eval(): division by zero` and must not be done. `validation/RAYLEIGH-ZERO-FINDINGS.md` §§3, 8 (revised in place). |
| I-11 | prompt 07 | `FabJ_eval` (`lib/detail/special_functions.cpp`, commit `5e2b208`) returns the closed form `Pi/(4 s t u) * P_n((t^2+u^2-s^2)/(2 t u))`, valid for all `n`, in place of the `n <= 4` table. `baseline/post-fix` was regenerated from the tree after `5e2b208`/`69cae4a` (binary built from `74bc284`) and the full comparison matrix run (§2 above): 101/101 signatures vs `baseline/pre-fix` and the 2018 tree, counterterm map and warning set identical, dedup unchanged at 101→95/6. Textually, exactly 47 of 101 `value_` expressions differ from `baseline/pre-fix` (confirmed count) and kernel numbering is permuted, because the backend sorts kernels by printed content; the sorted `Wick_` line set is byte-identical. `SUMMARY.md` §4 records this as a textual-not-functional change. See `logs/07-closeout-and-handover.md`. |

---

## 5. Deliberately deferred

Recorded so that they are not re-proposed inside this campaign. See `RECONCILIATION.md` §6. Checked
against reality at close-out (prompt 07): every item below was in fact left untouched throughout the
whole campaign, confirmed by the byte-identical/functionally-identical comparisons in §2.

- Tier 3 generalisations: multi-external-momentum Rayleigh reduction; three-or-more `LegP`
  products; new correlator containers. **Revised 2026-09-05:** the study's original claim that Tier
  3 would need a four-Bessel/adaptive-Levin quadrature path was withdrawn during the Rayleigh-zero
  resolution (I-03) — the reduction's forced integration order (Rayleigh direction first) means only
  the `FabJ(0,n,n)` two-Bessel family is ever produced, at every tier. See `validation/ORACLE.md` §4.
- `Bk` placeholder, three-field blocks, the 12-type cross ⟨b₁δ × b₂δ²⟩.
- Replacing the 27 hand-written bias filters with a single polynomial decomposition.
- Canonical-form (rational normal form) dedup of emitted kernels.
- `together`+`cancel`+CSE rewriting of the emitted arithmetic.
- The four-region asymptotic analysis and group-level IR summation.
- Intermediate-representation serialisation; fixed-grid quadrature; any SymPy work.

---

## 6. Log index

| Prompt | Log file | Written |
|---|---|---|
| 01 | `logs/01-deboost-and-build.md` | ✅ |
| 02 | `logs/02-oracle-harness-and-baseline.md` | ✅ |
| 03 | `logs/03-determinism.md` | ✅ |
| 04 | `logs/04-wick-tier1-fixes.md` | ✅ |
| 05 | `logs/05-numeric-and-shape-hygiene.md` | ✅ |
| 06 | `logs/06-rayleigh-zero-diagnosis.md` | ✅ |
| 07 | `logs/07-closeout-and-handover.md` | ✅ |

---

## 7. Campaign closed

All seven prompts are **Complete**. Both baselines are archived and tagged (`baseline/pre-fix`,
`baseline/post-fix`). The issue register is reconciled: I-01, I-03, I-04, I-07, I-08 and I-11 are
closed (§4); I-02, I-05, I-06, I-09 and I-10 remain open and are handed to the Python port via
`validation/ORACLE.md`. The campaign's own account of itself is `prompts/pre-python-migration-fixes/SUMMARY.md`;
the port author's handover is `validation/ORACLE.md`. Nothing in `RECONCILIATION.md` §6's
out-of-scope list was touched.
